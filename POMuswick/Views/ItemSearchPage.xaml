<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="POMuswick.Views.ItemSearchPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:POMuswick.Controls"
    xmlns:ffimageloading="clr-namespace:FFImageLoading.Maui;assembly=FFImageLoading.Maui"
    xmlns:fftransformations="clr-namespace:FFImageLoading.Transformations;assembly=FFImageLoading.Maui"
    xmlns:local="clr-namespace:POMuswick.ViewModels"
    Title="Search Products"
    HideSoftInputOnTapped="True"
    Shell.NavBarIsVisible="False"
    Shell.TabBarIsVisible="False">

    <!--<NavigationPage.TitleView>
        <StackLayout
            Orientation="Horizontal"
            Spacing="10"
            VerticalOptions="Center">

            <Label
                Padding="10,0,0,0"
                FontSize="16"
                Text="Muswick Wholesale Grocers"
                TextColor="White"
                VerticalTextAlignment="Center" />
        </StackLayout>
    </NavigationPage.TitleView>-->

    <!--
    <Shell.SearchHandler>
        <controls:MySearchHandler Placeholder="Search Products" ShowsResults="false" DisplayMemberName="Name" />
    </Shell.SearchHandler>
    -->

    <ContentPage.BindingContext>
        <local:ItemSearchViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="BannerBackground">White</Color>
            <Color x:Key="Accent">#96d1ff</Color>
        </ResourceDictionary>
        <!--  Template for mobile/small screens  -->
        <DataTemplate x:Key="SmallScreen">
            <controls:CustomSmallListItem />
        </DataTemplate>

        <!--  Template for tablets/large screens  -->
        <DataTemplate x:Key="LargeScreen">
            <controls:CustomListItem />
        </DataTemplate>

        <!--  The selector linking them together  -->
        <controls:ResponsiveTemplateSelector
            x:Key="MySelector"
            LargeTemplate="{StaticResource LargeScreen}"
            SmallTemplate="{StaticResource SmallScreen}" />
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid Margin="{OnPlatform iOS='0,50,0,0', Android='0'}" SafeAreaEdges="{OnPlatform iOS='None', Android='All'}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <!--  Header  -->
                <RowDefinition Height="Auto" />
                <!--  Search Box  -->
                <RowDefinition Height="Auto" />
                <!--  Category Labels (Fix: 120 is risky)  -->
                <RowDefinition Height="3" />
                <!--  Divider  -->
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <!--  Footer  -->
            </Grid.RowDefinitions>

            <controls:CustomHeader Grid.Row="0" />
            <StackLayout
                Grid.Row="1"
                HorizontalOptions="Center"
                Orientation="Horizontal"
                VerticalOptions="Center">
                <Entry
                    x:Name="Search"
                    Margin="0,0,0,0"
                    ClearButtonVisibility="WhileEditing"
                    Completed="OnTappedSearch"
                    FontSize="14"
                    HeightRequest="36"
                    HorizontalOptions="Start"
                    IsSpellCheckEnabled="False"
                    IsTextPredictionEnabled="False"
                    Placeholder="Enter Search Text"
                    PlaceholderColor="Gray"
                    Text="{Binding SearchText}"
                    VerticalOptions="Center"
                    WidthRequest="250" />
                <Image
                    Margin="10,0,0,0"
                    HeightRequest="20"
                    WidthRequest="20">
                    <Image.Source>
                        <FontImageSource
                            FontFamily="FontAwesomeFreeSolid"
                            Glyph="&#xF002;"
                            Size="12"
                            Color="Blue" />
                    </Image.Source>
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnTappedSearch" />
                    </Image.GestureRecognizers>
                </Image>
            </StackLayout>
            <StackLayout
                Grid.Row="2"
                Orientation="Vertical"
                Spacing="0"
                VerticalOptions="Start">
                <BoxView
                    BackgroundColor="gray"
                    HeightRequest="3"
                    HorizontalOptions="Fill"
                    VerticalOptions="Fill"
                    Color="lightgray" />
                <StackLayout
                    Margin="0,10,0,0"
                    HorizontalOptions="Center"
                    Orientation="Vertical"
                    VerticalOptions="Center">
                    <Label
                        Margin="0,0,0,0"
                        FontAttributes="Bold"
                        FontSize="16"
                        HorizontalOptions="Center"
                        Text="{Binding Category}"
                        TextColor="black"
                        TextDecorations="Underline">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnTappedCategory" />
                        </Label.GestureRecognizers>
                    </Label>
                    <Frame
                        Margin="10,0,0,0"
                        Padding="0,0,0,0"
                        BackgroundColor="#CDCDCD"
                        BorderColor="Transparent"
                        CornerRadius="5"
                        HeightRequest="20"
                        HorizontalOptions="Center"
                        IsVisible="false"
                        VerticalOptions="Center"
                        WidthRequest="20">
                        <Label
                            Margin="0,0,0,0"
                            BackgroundColor="#CDCDCD"
                            FontAttributes="Bold"
                            FontSize="12"
                            HeightRequest="20"
                            HorizontalTextAlignment="Center"
                            Text="X"
                            TextColor="Black"
                            VerticalTextAlignment="Center"
                            WidthRequest="20">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnTappedClearCategory" />
                            </Label.GestureRecognizers>
                        </Label>
                    </Frame>
                </StackLayout>
                <StackLayout
                    Margin="0,0,0,7"
                    HorizontalOptions="Center"
                    Orientation="Horizontal"
                    VerticalOptions="Center">
                    <CheckBox
                        x:Name="InStockOnly"
                        Margin="20,0,0,0"
                        CheckedChanged="InStockOnly_CheckedChanged"
                        IsChecked="{Binding InStockOnlyValue}"
                        VerticalOptions="Center"
                        Color="Blue" />
                    <Label
                        x:Name="InStockLabel"
                        Margin="0,2,0,0"
                        FontSize="12"
                        Text="Show In Stock Only"
                        TextColor="black"
                        VerticalOptions="Center" />
                </StackLayout>
                <StackLayout
                    Margin="0,0,0,5"
                    HorizontalOptions="Center"
                    IsVisible="false"
                    Orientation="Horizontal"
                    VerticalOptions="Center">
                    <Label
                        Padding="0,7,0,10"
                        FontAttributes="Bold"
                        FontSize="16"
                        HorizontalOptions="Center"
                        Text="{Binding Subcategory}"
                        TextColor="black"
                        TextDecorations="Underline">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnTappedSubcategory" />
                        </Label.GestureRecognizers>
                    </Label>
                    <Frame
                        Margin="10,0,0,0"
                        Padding="0,0,0,0"
                        BackgroundColor="#CDCDCD"
                        BorderColor="Transparent"
                        CornerRadius="5"
                        HeightRequest="20"
                        HorizontalOptions="Center"
                        IsVisible="false"
                        VerticalOptions="Center"
                        WidthRequest="20">
                        <Label
                            Margin="0,0,0,0"
                            BackgroundColor="#CDCDCD"
                            FontAttributes="Bold"
                            FontSize="12"
                            HeightRequest="20"
                            HorizontalTextAlignment="Center"
                            Text="X"
                            TextColor="Black"
                            VerticalTextAlignment="Center"
                            WidthRequest="20">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnTappedClearSubcategory" />
                            </Label.GestureRecognizers>
                        </Label>
                    </Frame>
                </StackLayout>
                <BoxView
                    Grid.Row="3"
                    BackgroundColor="gray"
                    HeightRequest="3"
                    HorizontalOptions="Fill"
                    VerticalOptions="Fill"
                    Color="lightgray" />
            </StackLayout>
            <CollectionView
                x:Name="ItemsListSearch"
                Grid.Row="4"
                ItemTemplate="{StaticResource MySelector}"
                SelectionChanged="ItemsListSearch_SelectionChanged"
                SelectionMode="Single"
                VerticalOptions="Fill" />
            <VerticalStackLayout
                x:Name="ImageOverlay"
                Grid.Row="0"
                Grid.RowSpan="5"
                Grid.Column="0"
                Padding="20"
                BackgroundColor="#80000000"
                IsVisible="False">
                <ffimageloading:CachedImage
                    x:Name="FullImage"
                    ErrorPlaceholder="appicon.png"
                    HeightRequest="500"
                    LoadingPlaceholder="appicon.png"
                    WidthRequest="300" />
                <Button
                    Clicked="Button_Clicked"
                    HorizontalOptions="Center"
                    Text="Close" />
            </VerticalStackLayout>
            <controls:CustomToolbar Grid.Row="5" />
        </Grid>
    </ContentPage.Content>
</ContentPage>
